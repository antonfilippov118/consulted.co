<div>
  <span ng-show="loading">
    <i class="fa fa-spinner fa-spin"></i>
  </span>
  <span ng-hide="loading">
    <a ng-hide="offer.expert.slug === '<%= @user.slug rescue '' %>'" ng-hide="<%= @user.nil? %>" ng-click="contact(offer.expert)" class="btn btn-primary">Contact</a>
  </span>
</div>
