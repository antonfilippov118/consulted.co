<div id="lookup">
  <div id="search">
    <form role="form" ng-submit="lookup()">
      <div id="search-box">
        <input datasets="data" options="options" type="text" ng-model="term" class="sfTypeahead form-control" placeholder="How can we help? E.g. &quot;Valuation&quot;, &quot;Growth Strategy&quot;, etc.">
      </div>
      <button type="submit" class="btn btn-primary" ng-disabled="searching" id="search-sub">Search</button>
      <div ng-show="searching" class="text-center">
        <br>
        <i class="fa fa-refresh fa-2x fa-spin"></i>
      </div>
      <div ng-show="!result && search_active && !searching" class="text-center">
        <h1>No results</h1>
      </div>
      <div class="results" ng-show="search_active && !searching && result">
        <h2>{{groups.length}} service offering{{groups.length != 1 ? 's' : ''}} found</h2>
        <div class="row">
          <div class="col-md-6" ng-repeat="group in groups | orderBy:'name'">
            <div class="result">
              <h3>{{group.name}}</h3>
              <div bread group="group" hide-group hide-root></div>
              <p>{{group.description}}</p>
              <div class="result-bottom">
                <a class="result-info" ng-click="learn(group)">
                  <span class="flaticon flaticon-info"></span>Learn more
                </a>
                <a ng-href="/groups/{{group.slug}}" class="btn btn-primary">Browse experts</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </form>
  </div>
</div>
