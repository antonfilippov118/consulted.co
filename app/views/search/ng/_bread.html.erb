<ul class="bread" ng-show="breadcrumbsActive()">
  <li>
    <a ng-href="#!/">Overview</a>
  </li>
  <li ng-repeat="crumb in crumbs">
    <a ng-href="#!/{{crumb.slug}}" ng-hide="$last">{{crumb.name}}</a>
    <span ng-show="$last">{{crumb.name}}</span>
  </li>
</ul>
