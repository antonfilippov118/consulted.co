<ul class="bread" ng-show="crumbs.length > 0">
  <li>
    <a ng-href="#!/">Find an expert</a>
  </li>
  <li ng-repeat="crumb in crumbs">
    <a ng-href="#!/{{crumb.slug}}">{{crumb.name}}</a>
  </li>
</ul>
