<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= favicon_link_tag  %>
    <title><%= content_for?(:title) ? yield(:title) : "CONSULTED.co" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "CONSULTED.co" %>">
    <!--[if lt IE 9]>
      <%= javascript_include_tag 'compatibility' %>
    <![endif]-->
    <%= stylesheet_link_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%# Main navigation %>
    <%= render partial: 'layouts/landing/navigation' %>
    <%= yield %>
    <%= render partial: 'layouts/landing/footer' %>
    <div id="copy">&copy; 2014 - consulted.co</div>
    <%= javascript_include_tag 'application' %>
    <%= javascript_include_tag 'landing' %>
  </body>
</html>