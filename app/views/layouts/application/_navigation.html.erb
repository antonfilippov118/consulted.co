<nav id="nav">
  <div class="container">
    <ul id="nav-tabs" class="nav nav-tabs">
      <%= content_tag :li, class: active_class(overview_path) do %>
        <% if user_signed_in? %>
          <%= link_to 'Overview', overview_path %>
        <% else %>
          <%= link_to 'Overview', new_user_registration_path, class: 'disabled' %>
        <% end %>
      <% end %>
      <%= content_tag :li, class: active_class(find_an_expert_path) do %>
        <%= link_to 'Find an expert', find_an_expert_path  %>
      <% end %>
      <%= content_tag :li, class: active_class(offers_path) do %>
        <% if user_signed_in? %>
          <%= link_to 'Offer your time', offers_path %>
        <% else %>
          <%= link_to 'Offer your time', new_user_registration_path, class: 'disabled' %>
        <% end %>
      <% end %>
    </ul>
    <ul id="nav-side">
      <li><a href="#">Your Calls</a></li>
      <li><a href="#">Bookmarked Experts</a></li>
    </ul>
  </div>
</nav>
<div id="navtoggle"><span class="navtoggle-menu">Menu</span> <span class="fa fa-bars"></span></div>
